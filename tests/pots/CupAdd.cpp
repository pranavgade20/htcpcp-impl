#include <iostream>

#include "Cup.h"

int main() {
Cup cup;
    if (cup.getDescription() = nullptr) {
        // Should not be null
        std::cout << "ERROR: getDescription should not be empty";
        return 1;
    }
    return 0;
} 
}

